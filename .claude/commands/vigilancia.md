Activa la skill `ticket-de-orden` en modo vigilancia (Paso 0-4 de su
`SKILL.md`, variante "Modo vigilancia"), usando la configuración guardada en
`.claude/configuracion.json`. Pensado para usarse con `/loop` (por ejemplo
`/loop 1h /vigilancia`): si no cambió nada relevante desde la última vez,
responde solo "Sin novedades." y nada más.
